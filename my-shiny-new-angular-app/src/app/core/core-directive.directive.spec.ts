import { CoreDirectiveDirective } from './core-directive.directive';

describe('CoreDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
