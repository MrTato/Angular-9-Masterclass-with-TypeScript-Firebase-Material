import { TransformDirective } from './transform.directive';

describe('TransformDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformDirective();
    expect(directive).toBeTruthy();
  });
});
